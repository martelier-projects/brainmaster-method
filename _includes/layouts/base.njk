<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{ title }}</title>

        <link rel="stylesheet" href="{{ '/statics/style/style.css' | url }}">
    </head>
    <body>
        <header>
            <h1>logo</h1>
            {% include "components/site-nav/site-nav.njk" %}
        </header>

        <main>
            {{ content | safe }}
        </main>

        <footer></footer>
    </body>
</html>